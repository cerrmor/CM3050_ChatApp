import React from 'react';
import './config/firebase';
import RootNavigation from './navigation';

//Main app file displays only the need page
export default function App() {
  return (
    <RootNavigation />
  );
}